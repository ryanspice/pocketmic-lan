I've read all five sources. The web folder doesn't exist yet, so I'll create it with the three files. I have no terminal tool, so I'll do equivalent well-formedness checks via file re-reads + grep, and report exactly which commands I could NOT run.
Continuing — creating the three files now.
<string_to_replace>]<]minimax[>[</string_to_replace>]<]minimax[>[</invoke>
]<]minimax[>[
session_id: 20260903_005459_b39f01