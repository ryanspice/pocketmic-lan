Verdict: The docs describe a designed-and-source-verified protocol, not a built or device-tested product — every headline claim the page could make (encrypted, low latency, works end-to-end) is either design intent or JVM-level smoke evidence, and the only artifacts that exist are debug/unsigned.
1. DO-NOT-CLAIM
- Physical end-to-end behaviour is untested: README.md:158 "end-to-end behaviour still needs physical Android/Windows/Wi-Fi/VB-CABLE testing"; corroborated PERFORMANCE_AUDIT.md:45 and BUILD_STATUS.md:32-33 (no physical mic, Windows output-device, firewall, VB-CABLE, or latency testing).
- No signed release, installer, or updater exists: README.md:157; only artifacts are the debug APK (README.md:42-44) and `PocketMic-v0.1.2-release-unsigned.apk` (README.md:75). The default build output is a debug-keystore APK, never a release.
- No build was actually executed in this environment: BUILD_STATUS.md:30-31 (Android SDK Platform 36 and .NET 8 SDK not installed); README.md:5 "It does not pretend that an APK or EXE was built". Do not claim "build passed" or "verified artifact".
- Exact boundary, quote verbatim: README.md:149 "Do not port-forward the receiver. Encryption protects packet contents and integrity; it does not make this a hardened internet voice service."
- Windows receiver only; manual IPv4 entry only (no discovery/QR); PCM uses more bandwidth than Opus; fixed prebuffer with no adaptive jitter/clock recovery: README.md:153-156; PERFORMANCE_AUDIT.md:41.
2. MUST-INCLUDE
- AES-256-GCM authenticated encryption; key = SHA-256(UTF-8(pairing key)); nonce = 8-byte session ID + 4-byte sequence; AAD = full 24-byte header: README.md:142-145, PROTOCOL.md:34-38.
- LAN-only scope, trusted private LAN, firewall Private networks only, default UDP port 49500: README.md:100,147.
- 48 kHz mono PCM16, 10 ms frames (480 samples / 960 plaintext bytes), 100 packets/sec, 768 kbit/s raw audio, exactly 1000-byte datagram: README.md:11, PROTOCOL.md:7-12,30.
- VB-CABLE virtual-mic routing (CABLE Input / CABLE Output) into Discord/Teams/OBS: README.md:84,90-96; artifact filenames `PocketMic-v0.1.2-debug.apk` / `PocketMicReceiver-win-x64.zip`: README.md:42-44; latency policy numbers 40 ms prebuffer / trim above 140 ms / ≤100 ms concealment: README.md:132-134.
3. CONTRADICTIONS
- README.md:37 says the default Android build "runs the JVM unit test, Android lint, and the requested APK build" vs BUILD_STATUS.md:30 — that pipeline never executed here (SDK 36 absent). "Pipeline defaults to running checks" ≠ "checks have passed"; copywriter must not fuse the two.
- PERFORMANCE_AUDIT.md:35 headline "well over 100,000 packets per second" is a JVM smoke run and the same line labels it "not an Android device latency benchmark"; a page citing that number as device throughput contradicts the doc's own scoping. Related trap: README.md:42-44 "Expected outputs" lists a version-named debug APK, so "release build" wording will be tripped by an artifact that is neither signed nor a release.
4. PARAPHRASE RISKS
- "End-to-end encrypted" → docs say "AES-256-GCM authenticated encryption" (README.md:12, PROTOCOL.md:35); link-level between two peers, no key exchange, key is a hash of a manually typed pairing key (README.md:142), and a reconnect deliberately reuses session/sequence (PROTOCOL.md:42). "E2E" or "secure key exchange" wording is false.
- "Low latency" with no number → no measured figure exists anywhere; only design numbers (README.md:132-134) explicitly "intentionally fixed and conservative" (README.md:138), and PERFORMANCE_AUDIT.md:40 refuses to claim any 10–20 ms target. Any latency figure must be labeled design intent.
- "App stays alive" → README.md:88 only means unconnected UDP sends continue when the receiver starts later (PERFORMANCE_AUDIT.md:16); real foreground/background phone behaviour is untested (BUILD_STATUS.md:32).
- "48 kHz audio" → dropping "mono PCM16" and "768 kbit/s raw" (README.md:11, PROTOCOL.md:7-12) implies stereo/hi-fi or compressed audio, none of which is true.
Next gate: lead must decide whether the page sells the protocol design (all claims provable from these docs) or the product (needs real device/artifact evidence that does not exist in v0.1.2).
Sharpest claim: v0.1.2 contains a protocol and a pipeline, not a product — any page that implies a working, built, tested, signed app is false by the docs' own words.
REFRAME: the landing page should market the wire format, not the binaries.
session_id: 20260903_005459_a5da19