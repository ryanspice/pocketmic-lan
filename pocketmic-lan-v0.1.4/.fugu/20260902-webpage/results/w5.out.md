Read complete. Report follows.
1. CLAIM VIOLATIONS
- Unqualified "low-latency" in three places — index.html:7 (title), :8 (meta description), :50 (tagline). No measured end-to-end latency exists anywhere in the source docs; the 10 ms figure on the page is a packet interval, not a latency measurement. w3.out.md:18 requires any latency claim be labeled design intent; w4b.out.md:43 forbids "unqualified 'low latency' without the numbers." The performance section does label its specific numbers correctly (:103, :112), but the hero/title adjective is unlabeled.
- Present-tense functional assertion — index.html:77 "Audio now streams encrypted 10 ms UDP frames to your PC" describes a working flow, while end-to-end behaviour is explicitly untested. w3.out.md:3; README.md:158. Mitigated (not negated) by limits at index.html:183.
- Boundary sentence paraphrased, not verbatim — index.html:129-130 replaces "does not make this a hardened internet voice service" with "does not make this a safe tool to expose to the public internet." w3.out.md:6 requires the exact boundary verbatim (README.md:149). The load-bearing phrase survives in the heading (:129), so this is minor.
- Checked, no violation: "encrypted" wording never says "end-to-end"; it correctly means AES-256-GCM link encryption (w3.out.md:17). Latency numbers (40/140/100 ms) are labeled design intent. No built/signed-artifact claim — outputs are labeled "Expected outputs" (:151), and the no-signed-release / untested limits are present.
2. OMISSIONS
- 768 kbit/s raw audio bandwidth — missing. w3.out.md:11; w1.out.md:23 (PROTOCOL.md:12).
- 480 samples / 960 plaintext bytes per packet — missing. w3.out.md:11; PROTOCOL.md:10.
- Exactly 1000-byte datagram — missing. w3.out.md:11; PROTOCOL.md:30.
- All other w3 must-includes are present: AES-256-GCM, key SHA-256(pairing key), nonce 64-bit session + 32-bit sequence, 24-byte-header AAD, LAN-only, default port 49500, Private networks, VB-CABLE (CABLE Input/Output), artifact filenames, 40 ms / 140 ms / ≤100 ms, six limits, boundary.
3. A11Y + STRUCTURE
- Mobile nav unreachable without JS — nav-list is display:none at ≤760px and only revealed via :has(.nav-toggle[aria-expanded="true"]), which requires JS to toggle; nav-toggle is `hidden` in markup and only unhidden by JS. styles.css:97-107; index.html:25; app.js:56. Desktop nav (width >760px) works without JS.
- Nav link text ≠ section heading text, against w4b.out.md:10 exact-match rule: "Features"→"What is included" (index.html:35 vs :84); "Performance"→"Performance profile" (:36 vs :102); "Security"→"Security & network boundary" (:37 vs :118); "Build & install"→"Build & install on Windows 11" (:38 vs :137); "Limits"→"Current limits" (:39 vs :175); "Overview" has no matching h1 (:32).
- Nav includes an #hero "Overview" link (index.html:32) against w4b.out.md:4 "hero needs no anchor; 6 links only"; and the anchor id is #build-install (index.html:135) vs the specified #build (w4b.out.md:4).
- color-scheme meta declares light-only (index.html:6), which suppresses prefers-color-scheme:dark, making the entire dark-mode block styles.css:283-296 dead code.
- Touch targets under 44px: nav-toggle ≈28px (styles.css:87-91), nav links ≈38px (styles.css:79-83). w4b.out.md:23.
- Passes: one h1 (index.html:49); no heading-level skips; header/nav[aria-label]/main#main/footer landmarks present; skip link first-in-body → #main (index.html:12,:44); :focus-visible present (styles.css:51); no outline:none; reduced-motion block present (styles.css:274-280); no <img>/alt issues (only aria-hidden SVG); nav-toggle has accessible name + aria-controls + aria-expanded; no tabindex>0; no aria-hidden on focusable content; accent #1f5dd6 on #fff ≈5.8:1 (AA pass).
4. VERDICT
PASS-WITH-FIXES. Fixes: (1) qualify or drop "low-latency" in title/meta/tagline — label design intent or say "10 ms UDP framing," not latency; (2) add 768 kbit/s raw audio, 480 samples/960 bytes, 1000-byte datagram; (3) make mobile nav usable without JS (don't hide nav-list, or use a CSS-only toggle); (4) align nav text to headings, drop #hero "Overview", rename anchor to #build; (5) resolve color-scheme conflict (meta "light dark" or delete the dark block); (6) restore the verbatim boundary quote; (7) bump nav touch targets to ≥44px.
Framing check: the page sells the protocol design honestly — real numbers, six limits, boundary, correct non-E2E "encrypted" wording, no built-artifact claim — but overclaims the product at exactly two spots: the unqualified "low-latency" headline and the present-tense "Audio now streams" step.
Sharpest claim: every number on the page is provable, and the single word "low-latency" in the hero is the page's only true overclaim — it asserts a latency that no measurement in the docs supports.
session_id: 20260903_023614_1f9e83